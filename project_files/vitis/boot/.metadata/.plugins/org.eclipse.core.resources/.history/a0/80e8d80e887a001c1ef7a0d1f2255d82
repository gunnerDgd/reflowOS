#include <cstdint>
#include <cstddef>

namespace reflowOS {
namespace boot 	   {

	template <typename ModuleType>
	ModuleType* load_module(ModuleType&&);

	template <typename ModuleType, typename ModuleCount>
	ModuleType* load_module(ModuleType(&)[ModuleCount]);
}
}
