#include <cstdint>
#include <cstddef>

#define STATIC(type) static constexpr type

namespace reflowOS {
namespace boot 	   {
	class memory
	{
	public:
		typedef std::size_t size_type;
		STATIC(std::size_t) section_size = 4096;
	};
}
}
