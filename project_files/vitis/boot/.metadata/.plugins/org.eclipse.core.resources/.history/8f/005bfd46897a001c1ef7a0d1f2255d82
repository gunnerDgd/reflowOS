#include <cstdint>
#include <cstddef>

#define STATIC(type) static constexpr type

namespace reflowOS {
namespace boot 	   {
	class memory
	{
	public:
		typedef std::size_t size_type;
		STATIC(size_type) section_size = 4096;
	};
}
}
