#pragma once

#include <reflow_firmware/memory/memory_types.hpp>
#include <reflow_firmware/memory/>

namespace reflow_firmware::memory {
	class memory_manager
	{
	public:
		void allocate  (size_type);
		void deallocate(size_type);
	};
}
